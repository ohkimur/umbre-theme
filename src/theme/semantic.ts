import type { ThemeModel } from "@/theme/model.ts";
import type { SemanticTokenColor } from "@/theme/types.ts";

export const semanticTokenColors = ({ syntax }: ThemeModel): Record<string, SemanticTokenColor> => ({
  namespace: syntax.foreground,
  class: syntax.class,
  "class.defaultLibrary": syntax.tag,
  enum: syntax.type,
  "enum.defaultLibrary": syntax.tag,
  interface: syntax.tag,
  "interface.defaultLibrary": syntax.tag,
  struct: syntax.class,
  "struct.defaultLibrary": syntax.tag,
  type: syntax.type,
  "type.defaultLibrary": syntax.tag,
  typeParameter: syntax.type,
  parameter: syntax.foreground,
  property: syntax.foreground,
  enumMember: syntax.regexp,
  event: syntax.operator,
  function: syntax.function,
  method: syntax.method,
  macro: syntax.special,
  label: syntax.special,
  comment: syntax.comment,
  string: syntax.string,
  keyword: syntax.keyword,
  number: syntax.constant,
  regexp: syntax.regexp,
  operator: syntax.operator,
  decorator: syntax.special,
  variable: syntax.foreground,
  "variable.readonly": syntax.foreground,
});
